"""safeRL executable modules and reusable experiment helpers.

Run command-line modules from the ``safeRL_workspace`` directory
with ``python -m scripts.<group>.<module>``. The package layout is described
in ``scripts/README.md`` and the notebook-facing name map lives in
``scripts/catalog.py``.
"""
